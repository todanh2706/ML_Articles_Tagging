<div class="space-y-8">
  <div class="text-center">
    <h1 class="text-3xl font-bold text-gray-900">So sánh hiệu suất mô hình</h1>
    <p class="mt-2 text-lg text-gray-600">Đánh giá và so sánh trên bài đơn lẻ và file CSV.</p>
  </div>

  <div class="bg-white rounded-xl shadow-sm border border-slate-200 p-6">
    <h2 class="text-xl font-semibold text-gray-800 mb-4">2.1 So sánh trên 1 bài báo</h2>
    <form class="space-y-4 mb-6" action="/compare/text" method="POST">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div class="md:col-span-2">
          <label for="compare-content" class="block text-sm font-medium text-gray-700">Nội dung bài báo (text)</label>
          <textarea id="compare-content" name="compare_text" rows="4"
            class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm border p-2"
            placeholder="Nhập nội dung...">{{compareText}}</textarea>
        </div>
        <div>
          <label for="ground-truth" class="block text-sm font-medium text-gray-700">Tag đúng (ground truth)</label>
          <input id="ground-truth" name="true_tag"
            class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm border p-2"
            value="{{trueTag}}" />
        </div>
        <div class="flex items-end">
          <button type="submit"
            class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
            So sánh các model
          </button>
        </div>
      </div>
    </form>

    <div class="border-t border-gray-100 pt-6 space-y-6">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
        {{#if textEvaluation.models}}
          {{#each textEvaluation.models}}
          <div class="bg-slate-50 p-4 rounded-lg border border-slate-200 relative overflow-hidden">
            <div class="absolute top-0 right-0 p-2">
              {{#if correct}}
              <svg class="h-6 w-6 text-green-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
              </svg>
              {{else}}
              <svg class="h-6 w-6 text-red-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
              </svg>
              {{/if}}
            </div>
            <h3 class="font-semibold text-gray-900">{{name}}</h3>
            <div class="mt-2">
              <span class="text-xs text-gray-500">Dự đoán:</span>
              <div class="text-lg font-bold {{#if correct}}text-indigo-600{{else}}text-red-600{{/if}}">{{predicted_tag}}</div>
            </div>
            {{#if error}}
            <div class="text-xs text-red-600">Error: {{error}}</div>
            {{/if}}
            <div class="mt-2">
              <span class="text-xs text-gray-500">Độ tin cậy (tag đúng):</span>
              <div class="text-sm font-medium">{{true_tag_prob}}</div>
            </div>
            {{#if softmaxList}}
            <div class="mt-3 space-y-1">
              {{#each softmaxList}}
              <div class="flex justify-between text-xs text-gray-700">
                <span>{{label}}</span>
                <span>{{prob}}</span>
              </div>
              <div class="w-full bg-gray-200 rounded-full h-2">
                <div class="bg-indigo-600 h-2 rounded-full" style="width: calc({{prob}} * 100%);"></div>
              </div>
              {{/each}}
            </div>
            {{/if}}
          </div>
          {{/each}}
        {{else}}
          <div class="col-span-3 text-sm text-gray-500">Nhập text và tag đúng để so sánh.</div>
        {{/if}}
      </div>

      {{#if textEvaluation.models}}
      <div>
        <h4 class="text-sm font-medium text-gray-500 mb-2">Xác suất cho tag đúng</h4>
        <div class="space-y-2">
          {{#each textEvaluation.models}}
          <div class="flex items-center">
            <span class="w-32 text-xs text-gray-600">{{name}}</span>
            <div class="flex-grow bg-gray-200 rounded-full h-2">
              <div class="bg-indigo-600 h-2 rounded-full" style="width: calc({{true_tag_prob}} * 100%);"></div>
            </div>
            <span class="ml-2 text-xs font-medium">{{true_tag_prob}}</span>
          </div>
          {{/each}}
        </div>
      </div>
      {{/if}}
    </div>
  </div>

  <div class="bg-white rounded-xl shadow-sm border border-slate-200 p-6">
    <h2 class="text-xl font-semibold text-gray-800 mb-4">2.2 So sánh trên tập CSV</h2>

    <form class="flex flex-col sm:flex-row gap-4 items-end border-b border-gray-100 pb-6 mb-6" action="/compare/csv"
      method="POST" enctype="multipart/form-data">
      <div class="flex-grow w-full sm:w-auto">
        <label for="compare-csv" class="block text-sm font-medium text-gray-700">Chọn file CSV (bài báo + tag đúng)</label>
        <input type="file" id="compare-csv" name="csv_file" class="mt-1 block w-full text-sm text-slate-500
                file:mr-4 file:py-2 file:px-4
                file:rounded-full file:border-0
                file:text-sm file:font-semibold
                file:bg-indigo-50 file:text-indigo-700
                hover:file:bg-indigo-100
              " />
      </div>
      <button type="submit"
        class="w-full sm:w-auto flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
        Upload và so sánh
      </button>
    </form>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-8">
      {{#if csvEvaluation.summary}}
        {{#each csvEvaluation.summary}}
        <div class="bg-white border border-slate-200 rounded-lg p-4 shadow-sm">
          <h3 class="text-sm font-medium text-gray-500">{{@key}}</h3>
          <div class="mt-2 flex justify-between items-end">
            <div>
              <div class="text-2xl font-bold text-gray-900">{{this.accuracy}}</div>
              <div class="text-xs text-green-600">Accuracy</div>
            </div>
            <div class="text-right">
              <div class="text-sm font-semibold text-gray-700">{{this.f1}}</div>
              <div class="text-xs text-gray-500">F1-Score</div>
            </div>
          </div>
        </div>
        {{/each}}
      {{else}}
        <div class="bg-white border border-slate-200 rounded-lg p-4 shadow-sm text-sm text-gray-500">
          Upload CSV để xem kết quả.
        </div>
      {{/if}}
    </div>

    <div>
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200">
          <thead class="bg-gray-50">
            <tr>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">ID</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Tiêu đề</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Tag đúng</th>
              {{#each modelNames}}
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">{{this}}</th>
              {{/each}}
            </tr>
          </thead>
          <tbody class="bg-white divide-y divide-gray-200">
            {{#if csvEvaluation.rows}}
              {{#each csvEvaluation.rows}}
              <tr>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">{{id}}</td>
                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">{{title}}</td>
                <td class="px-6 py-4 whitespace-nowrap"><span
                    class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-gray-100 text-gray-800">{{true_tag}}</span>
                </td>
                {{#each ../modelNames}}
                <td class="px-6 py-4 whitespace-nowrap">
                  {{#with (lookup ../modelMap this)}}
                    <span class="{{#if correct}}text-green-600{{else}}text-red-600{{/if}} font-bold">{{#if correct}}✓{{else}}✗{{/if}}</span>
                    <span class="text-xs text-gray-500">{{predicted_tag}}</span>
                  {{/with}}
                </td>
                {{/each}}
              </tr>
              {{/each}}
            {{else}}
              <tr>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">101</td>
                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">Biến động giá vàng...</td>
                <td class="px-6 py-4 whitespace-nowrap"><span
                    class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-gray-100 text-gray-800">Kinh tế</span></td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500" colspan="3">Upload CSV để xem kết quả.</td>
              </tr>
            {{/if}}
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

<script src="//unpkg.com/alpinejs" defer></script>
