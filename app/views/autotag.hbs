<div class="space-y-8">
  <div class="text-center">
    <h1 class="text-3xl font-bold text-gray-900">Gán tag tự động</h1>
    <p class="mt-2 text-lg text-gray-600">Chọn chuyên mục để crawl từ backend và xem dự đoán của các model.</p>
  </div>

  <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-7 gap-4">
    {{#each tags}}
    <a href="/autotag/tag/{{this.key}}">
      <button
        class="py-4 px-5 text-base rounded-lg font-semibold shadow-sm focus:outline-none transition-colors duration-200 {{#if (eq ../selectedTag this.key)}}bg-indigo-600 text-white ring-2 ring-offset-2 ring-indigo-500{{else}}bg-white text-gray-700 hover:bg-gray-50 border border-gray-300{{/if}}">
        {{this.label}}
      </button>
    </a>
    {{/each}}
  </div>

  {{#if selectedTag}}
  <div class="space-y-6">
    <div class="flex flex-col sm:flex-row justify-between items-center bg-white p-4 rounded-lg shadow-sm border border-slate-200">
      <div class="flex items-center space-x-4 w-full sm:w-auto mb-4 sm:mb-0">
        <span class="text-sm text-gray-500">Đang xem: <span class="font-bold text-gray-900">{{selectedTag}}</span></span>
        <span class="text-xs text-gray-400">({{articles.length}} bài)</span>
      </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {{#if articles.length}}
      {{#each articles}}
      <div class="bg-white rounded-xl shadow-sm border border-slate-200 overflow-hidden hover:shadow-md transition-shadow duration-200 flex flex-col">
        <div class="p-6 flex-grow space-y-3">
          <div class="flex justify-between items-start">
            <div class="space-y-1">
              <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-indigo-100 text-indigo-800">{{predicted_tag}}</span>
              <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-emerald-100 text-emerald-800">consensus: {{consensus_hits}}</span>
              <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">model: {{model}}</span>
            </div>
            <div class="text-right text-xs text-gray-500">{{published_at}}</div>
          </div>
          <div class="flex items-center space-x-2 text-xs text-gray-600">
            <span class="font-semibold text-indigo-600">Confidence {{confidence}}</span>
            {{#if match}}
            <span class="text-emerald-600 font-semibold">Match</span>
            {{else}}
            <span class="text-amber-600 font-semibold">Off-tag</span>
            {{/if}}
          </div>
          <h3 class="text-lg font-bold text-gray-900 line-clamp-2">{{title}}</h3>
          <p class="text-sm text-gray-500 line-clamp-3">{{snippet}}</p>
        </div>
        <div class="bg-gray-50 px-6 py-4 border-t border-gray-100">
          <div class="flex justify-between items-center text-xs text-gray-500">
            <span>Nguồn: {{source}}</span>
            <a href="{{url}}" target="_blank" rel="noopener noreferrer"
              class="text-sm font-medium text-indigo-600 hover:text-indigo-500">Đọc bài báo &rarr;</a>
          </div>
        </div>
      </div>
      {{/each}}
      {{else}}
      <div class="col-span-3 text-sm text-gray-500">Chưa có bài phù hợp. Thử tag khác hoặc giảm ngưỡng.</div>
      {{/if}}
    </div>
  </div>
  {{else}}
  <div class="text-center text-sm text-gray-500">Chọn một tag để bắt đầu.</div>
  {{/if}}
</div>

<script src="//unpkg.com/alpinejs" defer></script>
