<nav class="bg-white shadow-sm border-b border-slate-200">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex">
        <div class="flex-shrink-0 flex items-center">
          <span class="font-bold text-xl text-indigo-600">ArticleTag Lab</span>
        </div>
        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
          <a href="/experiment"
            class="inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium {{#if (eq activePage 'experiment')}}border-indigo-500 text-indigo-600{{else}}border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700{{/if}}">
            Thực nghiệm mô hình
          </a>
          <a href="/compare"
            class="inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium {{#if (eq activePage 'compare')}}border-indigo-500 text-indigo-600{{else}}border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700{{/if}}">
            So sánh hiệu suất
          </a>
          <a href="/autotag"
            class="inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium {{#if (eq activePage 'autotag')}}border-indigo-500 text-indigo-600{{else}}border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700{{/if}}">
            Gắn tag tự động
          </a>
        </div>
      </div>
    </div>
  </div>
</nav>
