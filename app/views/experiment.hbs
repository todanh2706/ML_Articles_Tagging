<div class="space-y-8">
  <div class="text-center">
    <h1 class="text-3xl font-bold text-gray-900">Thực nghiệm mô hình</h1>
    <p class="mt-2 text-lg text-gray-600">Nhập bài báo để xem dự đoán và softmax từ các model.</p>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
    <div class="bg-white rounded-xl shadow-sm border border-slate-200 p-6">
      <h2 class="text-xl font-semibold text-gray-800 mb-4">1.1 Dự đoán từ văn bản</h2>
      <form action="/experiment/text" method="POST" class="space-y-4">
        <div>
          <label for="content" class="block text-sm font-medium text-gray-700">Nội dung bài báo (text)</label>
          <textarea id="content" name="article_text" rows="6"
            class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm border p-2"
            placeholder="Nhập nội dung bài báo...">{{inputText}}</textarea>
        </div>
        <button type="submit"
          class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
          Dự đoán tag
        </button>
      </form>

      <div class="mt-6 border-t border-gray-100 pt-4">
        <h3 class="text-sm font-medium text-gray-500 uppercase tracking-wider mb-3">Kết quả dự đoán</h3>

        {{#if textResult.models}}
        <div class="flex items-center space-x-4 mb-4">
          <span class="text-sm text-gray-600">Tag gợi ý:</span>
          <span
            class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-green-100 text-green-800">
            {{topTag}}
          </span>
        </div>
        <div class="space-y-3">
          {{#each textResult.models}}
          <div class="bg-slate-50 p-3 rounded-lg">
            <div class="flex justify-between text-sm font-medium text-gray-700 mb-1">
              <span>{{name}}</span>
              <span>{{predicted_tag}}</span>
            </div>
            {{#if softmaxList}}
            {{#each softmaxList}}
            <div class="mt-2">
              <div class="flex justify-between text-xs text-gray-700 mb-1">
                <span>{{label}}</span>
                <span>{{prob}}</span>
              </div>
              <div class="w-full bg-gray-200 rounded-full h-2">
                <div class="bg-indigo-600 h-2 rounded-full" style="width: calc({{prob}} * 100%);"></div>
              </div>
            </div>
            {{/each}}
            {{/if}}
          </div>
          {{/each}}
        </div>
        {{else}}
        <div class="text-sm text-gray-500">Nhập nội dung để xem kết quả.</div>
        {{/if}}
      </div>
    </div>

    <div class="bg-white rounded-xl shadow-sm border border-slate-200 p-6">
      <h2 class="text-xl font-semibold text-gray-800 mb-4">1.2 Dự đoán từ link bài báo</h2>
      <form action="/experiment/url" method="POST" class="space-y-4">
        <div>
          <label for="url" class="block text-sm font-medium text-gray-700">URL bài báo</label>
          <input type="url" id="url" name="article_url" value="{{urlInput}}"
            class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm border p-2"
            placeholder="https://example.com/bai-bao">
        </div>
        <button type="submit"
          class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
          Lấy nội dung và dự đoán
        </button>
      </form>

      <div class="mt-6 border-t border-gray-100 pt-4">
        <h3 class="text-sm font-medium text-gray-500 uppercase tracking-wider mb-3">Kết quả trích xuất</h3>
        {{#if urlResult.title}}
        <div class="bg-slate-50 p-4 rounded-lg mb-4">
          <h4 class="font-medium text-gray-900 truncate">{{urlResult.title}}</h4>
          <p class="text-sm text-gray-500 mt-1">{{urlResult.snippet}}</p>
        </div>
        {{/if}}
        <div class="flex flex-wrap gap-2">
          {{#if urlResult.models}}
            {{#each urlResult.models}}
            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
              {{name}}: {{predicted_tag}}
            </span>
            {{/each}}
          {{else}}
            <span class="text-sm text-gray-500">Nhập URL để xem kết quả.</span>
          {{/if}}
        </div>
      </div>
    </div>
  </div>

  <div class="bg-white rounded-xl shadow-sm border border-slate-200 p-6">
    <h2 class="text-xl font-semibold text-gray-800 mb-4">1.3 Dự đoán hàng loạt từ file CSV</h2>
    <form class="flex flex-col sm:flex-row gap-4 items-end border-b border-gray-100 pb-6 mb-6" action="/experiment/csv"
      method="POST" enctype="multipart/form-data">
      <div class="flex-grow w-full sm:w-auto">
        <label for="csv-file" class="block text-sm font-medium text-gray-700">Chọn file CSV (nhiều bài báo)</label>
        <input type="file" id="csv-file" name="csv_file" class="mt-1 block w-full text-sm text-slate-500
                file:mr-4 file:py-2 file:px-4
                file:rounded-full file:border-0
                file:text-sm file:font-semibold
                file:bg-indigo-50 file:text-indigo-700
                hover:file:bg-indigo-100
              " />
        <p class="mt-1 text-xs text-gray-500">Định dạng: id, title, content</p>
      </div>
      <button type="submit"
        class="w-full sm:w-auto flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
        Upload và dự đoán
      </button>
    </form>

    <div class="overflow-x-auto">
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">ID</th>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Tiêu đề</th>
            {{#each modelNames}}
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">{{this}}</th>
            {{/each}}
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          {{#if csvResult.rows}}
          {{#each csvResult.rows}}
          <tr>
            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">{{id}}</td>
            <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900 truncate max-w-xs">{{title}}</td>
            {{#each ../modelNames}}
            <td class="px-6 py-4 whitespace-nowrap">
              {{#with (lookup ../modelMap this)}}
                <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-green-100 text-green-800">{{predicted_tag}}</span>
              {{/with}}
            </td>
            {{/each}}
          </tr>
          {{/each}}
          {{else}}
          <tr>
            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">1</td>
            <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900 truncate max-w-xs">Phân tích thị trường chứng khoán...</td>
            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500" colspan="3">Upload CSV để xem kết quả.</td>
          </tr>
          {{/if}}
        </tbody>
      </table>
    </div>
  </div>
</div>

<script src="//unpkg.com/alpinejs" defer></script>
